# 접근성 — 이 디자인 시스템을 쓰는 앱이 인증을 잃지 않으려면

FactChat 은 **KWCAG 2.1**(한국형 웹 콘텐츠 접근성 지침 2.1) 기준의 **웹 접근성
품질인증**을 유지해야 하고, 그 앱 화면의 대부분은 이 저장소의 컴포넌트로 그려집니다.
그래서 여기서 난 결함 하나는 앱 한 곳이 아니라 **그 컴포넌트를 쓰는 모든 화면**에서
납니다.

이 문서는 규정 요약이 아니라, **이 저장소에서 실제로 났던 결함 아홉 가지가 어떤
모양이었는지**입니다. 아홉 중 셋이 같은 원인이었고, 전부 리뷰에서 보이지 않았습니다 —
화면은 멀쩡했고, 접근성 트리와 직렬화된 마크업만 다른 말을 하고 있었습니다.

---

## 1. 파트를 렌더하지 않으면 "레이블이 없는" 것이 아니라 **이름이 없는** 것이다

**아홉 중 셋(`SelectField` · `ComboboxField` · `Slider`)이 이것 하나였습니다.**

Chakra/Zag 의 파트 조합은 트리거에 `aria-labelledby="…:label"` 을 **레이블 파트를
렌더했든 안 했든 언제나** 내보냅니다. 파트를 빼면 그 참조는 존재하지 않는 id 를
가리키고, 접근 가능한 이름 계산은 **거기서 끝납니다** — 보이는 글자로 넘어가지
않습니다. 결과는 "레이블 없는 셀렉트"가 아니라 **이름이 아예 없는 버튼**입니다.
선택된 옵션이 화면에 크게 적혀 있어도 스크린 리더는 "버튼" 한 마디만 읽습니다.

```tsx
// ✗ 트리거에 이름이 없다 (axe `button-name`, KWCAG 5.3.4.1 레이블 제공)
<Select.Root>
  <Select.Control>
    <Select.Trigger>…</Select.Trigger>
  </Select.Control>
</Select.Root>;

// ✓ 레이블 파트를 언제나 렌더한다 — 이름이 화면 밖에 있으면 시각적으로만 숨긴다
{
  label != null ? (
    <Select.Label>{label}</Select.Label>
  ) : (
    (ariaLabel ?? placeholder) && (
      <Select.Label srOnly>{ariaLabel ?? placeholder}</Select.Label>
    )
  );
}
```

**속성이 아니라 파트로 다는 이유.** `aria-label` 을 하이드레이션 이후에 붙이면 그
전까지 컨트롤은 이름이 없습니다. 그 창은 이론이 아닙니다 — FactChat 의 감사가
`/dashboard/help` 에서 간헐적 `button-name` 위반으로 잡았고, 따뜻한 로드에서는 초록,
차가운 로드에서는 빨강이라 재현되지 않았습니다. 서버가 보낸 HTML 안에 이름이 있으면
그 창 자체가 없습니다.

**새 컴포넌트를 만들 때.** 파트 조합(`Select` · `Combobox` · `Slider` · `Menu` ·
`Popover` · `Tabs` …)을 감싸는 컴포넌트를 추가한다면, **이름이 어디서 오는지 먼저
정하십시오.** 호출부가 보이는 레이블을 주지 않는 경우가 있다면 — 표의 열 머리글,
`FieldRow`, 옆의 시간 표시가 이름인 경우 — `ariaLabel` 을 받아 숨은 파트로 렌더하는
것이 이 저장소의 방식입니다.

**타입으로 막지는 못하고 있습니다.** `label` · `ariaLabel` · `placeholder` 를 하나도
주지 않은 조합은 여전히 컴파일됩니다. 다운스트림(FactChat)의 래퍼는 유니온 타입으로
그것을 금지하고 있고, 그래서 **앱이 디자인 시스템보다 엄격합니다.** 여기서 타입을
좁히기 전까지 그 래퍼는 지우면 안 됩니다.

## 2. 글자 크기로 고른 컴포넌트가 문서 구조를 정하면 안 된다

`Subtitle` 은 `Text as="h6"` 였습니다. 그래서 모든 호출부가 — 제목 아래 한 줄짜리
보조 설명까지 — 문서 개요에 레벨 6 제목으로 들어갔고, `<h1>` 다음에 `<h6>` 가 오는
건너뜀이 생겼습니다(axe `heading-order`, KWCAG 5.2.4.2 제목 제공).

- **타이포그래피 컴포넌트의 기본 태그는 의미가 아니라 크기를 따른다.** `H1`~`H5` 는
  제목이니 제목 태그가 맞고, `Subtitle` · `Subtext` · `Caption` 은 아닙니다.
- **제목이 필요한 호출부는 호출부가 말한다** — `<Subtitle as="h2">`. 그 자리가 몇
  번째 수준인지는 그 자리에서만 알 수 있습니다.
- **반대 방향도 결함이다.** 마크다운 매퍼의 `h6` 는 **글쓴이가 실제로 쓴 제목**이라
  `as="h6"` 를 명시해야 합니다. 기본값을 문단으로 바꾸면서 이것까지 끌려가면, 고치던
  결함이 방향만 바꿔 다시 생깁니다.

## 3. `role` 은 그 요소에 허용되는 것이어야 한다

- `role="group"` 을 `<label>` 에 준 적이 있습니다. 허용되지 않는 조합이고(axe
  `aria-allowed-role`), 게다가 의도했던 호버 효과도 동작하지 않았습니다 — Chakra 의
  `_groupHover` 는 `.group:hover &` 로 컴파일되므로 **클래스**가 필요합니다.
- `role="button"` 을 실제 컨트롤을 감싼 래퍼에 준 적이 있습니다. 상호작용 요소 안에
  상호작용 요소가 들어가는 상태(axe `nested-interactive`)이고, 스크린 리더가 읽을
  방법이 없습니다.

**규칙:** 역할을 주기 전에 "이 요소가 정말 그것인가"를 묻고, 스타일 훅이 필요한
것이라면 role 이 아니라 클래스입니다.

## 4. 생성된 id 는 요소마다 달라야 한다

`RadioControl` 이 `RadioGroup.ItemControl` 안에 `ItemIndicator` 를 넣는데, Chakra 가
둘의 id 를 같은 `ids.itemControl` 에서 파생시켜 **라디오 하나가 같은 id 를 두 개**
내보내고 있었습니다.

WCAG 2.2 는 SC 4.1.1(Parsing)을 삭제했지만 **KWCAG 2.1 의 5.4.1.1 마크업 오류 방지는
살아 있는 검사항목**이고, 한국 심사원은 여전히 중복 id 를 오류로 셉니다. 게다가
`getElementById` 가 먼저 나온 쪽을 돌려주는 것은 그 자체로 버그입니다.

## 5. `<button>` 안에는 phrasing content 만 들어간다

`<div>` 나 `<p>` 를 `<button>` 안에 넣으면 중첩 오류입니다(KWCAG 5.4.1.1 의 중첩
관계). **브라우저가 조용히 고쳐 주기 때문에 DOM 을 훑는 어떤 검사도, axe 도 보지
못합니다** — 마크업 검사기만 봅니다. 아이콘을 감싸는 래퍼, 배지, 트리거 안의 라벨은
`as="span"` 으로 두고 `display` 를 명시하십시오. 레이아웃은 그대로입니다.

## 6. 확인하는 법

```bash
yarn test    # src/components/a11y.kwcag.test.tsx 가 위의 결함들을 붙잡고 있고,
             # 같은 파일의 axe 절이 구조·ARIA 규칙을 조합 위에서 돌린다
```

`yarn test` 는 `on-pr.yml` 이 매 PR 에 돌립니다. **컴포넌트를 새로 만들거나 파트
조합을 바꾸면 `a11y.kwcag.test.tsx` 의 axe 목록에 한 줄 추가하십시오** — 이름 없는
컨트롤, 허용되지 않는 role, 중첩된 상호작용 요소는 거기서 걸립니다.

jsdom 에는 레이아웃이 없어서 **명도 대비는 여기서 잴 수 없습니다.** 그쪽은
FactChat 의 `storybook-a11y.yml` 이 실제 브라우저에서 라이트·다크 양쪽으로 돌립니다.

## 더 읽을 것

- 24개 검사항목과 각각을 무엇이 붙잡고 있는지: FactChat 의 `docs/kwcag-compliance.md`
- 이 저장소가 소유한 결함과 앱 쪽 우회의 현재 상태: FactChat 의 `audits/_roadmap.md`
  § "KWCAG 2.1 defects owned by the design system"
- 원문: <http://www.kwacc.or.kr/WAI/wcag21/>
